# Ben agent phone book

Cursor's handle is always `@Cursor` (`<@U0C0BNM7VB6>`). There is no separate Lola or Kit bot.

Named people are **home channels + these lines**.

## Home

| Agent | Home channel | ID |
|---|---|---|
| **Lola** | `#ben-support` | `C0BV6LP1X8F` |
| **Kit** | `#ben-engineering-support-tickets` | `C0C0CAR1P9S` |

Talk to them there with:

```text
<@U0C0BNM7VB6>
```

That is a follow-up to the agent who already lives in that channel.

## Bring someone into another thread (paste exactly)

**Lola**

```text
<@U0C0BNM7VB6> agent You are Lola. Follow docs/agents/lola.md
```

**Kit**

```text
<@U0C0BNM7VB6> agent You are Kit. Follow docs/agents/kit.md
```

Then ⋯ on that agent's reply → **Add follow-up**. Do not mention `@Cursor` again in the thread unless you mean a new agent.

## Do not clone

Do not `<@U0C0BNM7VB6>` at the top of a home channel once that agent already exists. That starts a clone.

## Personas

- Lola: `docs/agents/lola.md`
- Kit: `docs/agents/kit.md`
